name: Manual Update Content

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run-scraper:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          npm install
          npm install openai

      - name: Run Mac Script
        env:
          VOLC_API_KEY: ${{ secrets.VOLC_API_KEY }}
          ENDPOINT_ID: ${{ secrets.ENDPOINT_ID }}
        run: |
          node mac-script.js

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "action@github.com"
          
          # 检查是否有任何文件发生变化
          if [[ -n $(git status -s) ]]; then
            echo "发现变动，准备提交..."
            
            # 🔴 核心修复 1：使用 git add . 包含所有变动文件 (包括 package-lock.json)
            git add .
            
            git commit -m "🤖 Auto-update: Added new Mac apps"
            
            # 🔴 核心修复 2：先拉取远程最新代码 (rebase)，解决冲突
            # 使用 --rebase --autostash 可以自动暂存脏数据，防止报错
            git pull --rebase --autostash origin main
            
            git push
            echo "✅ 成功推送！"
          else
            echo "没有新数据。"
          fi