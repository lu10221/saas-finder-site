name: Update All Data

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run-scrapers:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          npm install
          npm install openai

      - name: Run All Scripts
        env:
          VOLC_API_KEY: ${{ secrets.VOLC_API_KEY }}
          ENDPOINT_ID: ${{ secrets.ENDPOINT_ID }}
        run: |
          echo "🚀 Starting Mac Script..."
          node mac-script.js
          
          echo "🚀 Starting Self-Hosted Script..."
          node selfhosted-script.js
          
          # 如果你的 ai 脚本也改好了支持环境变量，也可以取消下面这行的注释
          # echo "🚀 Starting AI Script..."
          # node 2-generate-doubao.js

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "action@github.com"
          
          if [[ -n $(git status -s) ]]; then
            echo "发现新数据，准备提交..."
            
            # 把所有新生成的 JSON 文件都加进去
            git add .
            
            git commit -m "🤖 Auto-update: Content refreshed"
            
            # 自动处理冲突
            git pull --rebase --autostash origin main
            
            git push
            echo "✅ 所有数据更新完成！"
          else
            echo "没有发现新数据。"
          fi